# 02 — Bookshelf API v2

**Concepts covered:** Pydantic `BaseModel`, `Field` validation (`gt`, `lt`, `min_length`), `Optional` fields, `Path` and `Query` parameter validation, HTTP status codes (`201`, `200`, `204`), `HTTPException` with proper error responses, auto-incrementing ID generation, Swagger schema examples via `model_config`.

**Project:** An upgraded Bookshelf API built with FastAPI — extending the original CRUD foundation with strict request validation, proper HTTP status codes, structured error handling, and a Pydantic-based request model replacing raw `Body()` calls.

**Status:** Complete
